kind: pipeline
type: kubernetes
name: build_and_push
platform:
  os: linux
  arch: arm64
steps:
- name: build
  image: repo-docker-hosted.angeloxx.lan/angeloxx/drone/plugins/galaxy:latest
  settings:
    key: GITHUB_TOKEN
  when:
    branch:
    - master
    - development
  trigger:
  event:
  - push
  - custom
  branch:
  - master
  - development
